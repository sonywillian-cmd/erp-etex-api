# Bot de Telegram — instancia ${INSTANCIA} (generado por crear-instancia.sh)
# Token, Gemini y nombre se leen de Ajustes → Bot de Telegram; estos valores son solo el respaldo.
ERP_API_URL=http://127.0.0.1:${PUERTO_API}/api/v1
ERP_WEB_URL=https://${DOMINIO}
TELEGRAM_BOT_SHARED_SECRET=${TELEGRAM_BOT_SHARED_SECRET}
TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
GEMINI_API_KEY=${GEMINI_API_KEY}
GEMINI_MODEL=${GEMINI_MODEL}
FOTO_UPLOAD_DIR=${DIR_INSTANCIA}/uploads/gastos
FOTO_BASE_URL=https://${DOMINIO}/uploads/gastos
